---
title: "EDRM 718 Exercise Set K"
author: "Adam Brown"
date: "`r Sys.Date()`"
format: html
---

*Put your name as the author of this notebook in the YAML, then complete each of the items below. Make sure that your notebook is neat and organized and that all your code chunks run without error, even after clearing the environment. (Six points per item)*

***

*These exercises will lead you through the steps of preparing the **College Study Data** for analysis by cleaning it and importing it into R.*

```{r}

library(tidyverse)
library(here)
library(dplyr)
library(readr)
library(tibble)

```


***

**1. Import the *College Study Data* into a tibble and store the problems in a second tibble.**


```{r}

# Import the College Study Data CSV file into a tibble.

college_data <- read_csv(here("data", "College Study Data.csv"))

# Identify rows with problems and store in a second tibble.

college_problems <- problems(college_data)

# View the results
college_data
college_problems

```



***

**2. Display the rows in the imported data that contained identified problems. (Hint: Every row is considered during the import, including the header row, so row numbers in your imported data will not match the original row numbers that are identified in the problems tibble.)**

```{r}

college_data[college_problems$row - 1, ]

```


***

**3. Display these same rows in the original data, prior to import. (Hint: The basic R read.csv() function does not look for problems or make adjustments, so one trick is to read the same data into another object using this function.)**

```{r}

college_data_orginal <- read.csv(here("data", "College Study Data.csv"))

college_data_orginal[college_problems$row -1, ]

```


***

**4. Compare the two different displays of problem rows. For each row, state what the problem appears to have been and explain how the *read_csv()* function addressed the problem.**


*** A % was entered for scale 1 for id 7415 it was changed to missing data because it is not numeric. For id 10274, the data shifted left because id was missing and it marked scale2 and id as missing data. For 12225, 4b was incorrectly entered and was marked as missing data.

***

**5. For each problem row, explain whether you agree or disagree with how *read_csv()* handled the issue. Briefly defend each agreement and/or disagreement.**

*** For 7415, I don't know what the value should be, even though it might be a 7 because of the percent sign, but missing data is correct because we don't know for sure.

*** For 10274, we will shift everything to the left and then fill in the missing id number, all metrics appear to be within the correct ranges in the codebook.

*** For 12225, the extra b is most likely an extra key hit with 4. 


***

**6. If you disagreed with any of the solutions used by the *read_csv()* function, change the data in the problem row(s) to what you believe is correct. Show these row(s) when you are done.**

```{r}

college_data$id[10274] <- 10274
college_data$gender[10274] <- "male"
college_data$class[10274] <- "junior"
college_data$gpa[10274] <- 2.68
college_data$scale1[10274] <- 3
college_data$scale2[10274] <- 8
college_data[10274, ]

```


```{r}

college_data$scale2[12225] <- 4
college_data[12225, ]

```


***

**7. Note the types of data that the parse guesser chose for each variable. Identify which of these are incorrect and briefly explain why these are incorrect.**

*** Gpa should be double and not a character. Scales 1&2 should be integers, as in whole numbers. 

***

**8. You have already fixed some of the problems, so use the *write_csv()* function to save the data with the revisions you have made to this point. You should never delete original files, so use a different file name.**

```{r}


# Write the corrected data to a new CSV file in the data folder.

write_csv(college_data, here("data", "college_data_fixed.csv"))

```


***

**9. Clear your environment and read back in the file you just saved. This time specify the *col_types* argument so that each variable is set to the type you believe it should be. Store the problems in a tibble.**

```{r}

rm(list = ls())

# Read the fixed CSV file with correct column types.

college_reimport <- read_csv(here("data", "college_data_fixed.csv"),
    col_types = cols(col_integer(),
                     col_character(),
                     col_character(),
                     col_double(),
                     col_integer(),
                     col_integer()))

# Store any problems in a tibble
college_reimport_problems <- problems(college_reimport)

```


***

**10. As you did before, compare the problem rows with the original data. For each problem, explain what you believe the problem to be and whether you are satisfied with the solution used by *read_csv()*. If not, state how you propose to fix it.**

```{r}

# Import the College Study Data CSV file into a tibble.

college_data_orginal <- read_csv(here("data", "College Study Data.csv"))

college_data_orginal[college_reimport_problems$row - 1, ]


```


```{r}

college_reimport[college_reimport_problems$row - 1, ]

```


*** For 4551, I'm satisfied with missing data. On 11780, this is most likely 2.80, so we will retain 2.8.


***

**11. If you proposed any changes, go ahead an make them and show the result. If not, you can skip this item.**

```{r}

college_reimport$gpa[11780] <- 2.80

```


***

**12. Check that the identification numbers have a reasonable range and that they are all unique.**

```{r}

# Check the range of ID values
range(college_reimport$id, na.rm = TRUE)

# Check for duplicate IDs
anyDuplicated(college_reimport$id)

# Count of unique IDs
length(unique(college_reimport$id))

# Count of total IDs (excluding NAs)
sum(!is.na(college_reimport$id))

```
*** yes, these are unique and run from 1 - 14721.

***

**13. When these data were collected some time ago, it was common to consider only two gender types. Check that this is the case for these data. If there are inconsistencies, find the offending rows and display them. (Hint: The *table()* function is helpful when studying categorical data.)**


```{r}

# View frequency of each gender value
table(college_reimport$gender, useNA = "ifany")

# Display rows with unexpected or missing gender values
college_reimport %>%
  filter(!gender %in% c("male", "female") | is.na(gender))

```



***

**14. Fix any gender inconsistencies by changing the data in the way you believe makes the most sense. When you are done, display the corrected rows and again check the gender variable.**

```{r}


college_reimport_fixed <- college_reimport %>%
  mutate(
    gender = case_when(
      id == 3020 ~ "female",
      id == 5513 ~ "female",
      id == 8963 ~ "male",
      TRUE ~ gender
    ),
    class = if_else(id == 3020, "senior", class)
  )

```


***

**15. Use a similar procedure to check the *class* variable. If any changes are needed, fix them and then show that you have fixed the problems.**

```{r}

# View frequency of each class value
table(college_reimport_fixed$class, useNA = "ifany")

# Display rows with unexpected or missing class values
college_reimport_fixed %>%
  filter(!class %in% c("freshman", "sophomore", "junior", "senior") | is.na(class))

```

```{r}

college_reimport_fixed$class[college_reimport_fixed$class == "junirr"] <- "junior"

college_reimport_fixed$class[college_reimport_fixed$class == "SENIOR"] <- "senior"

table(college_reimport_fixed$class)


```



***

**16. Check the range of the GPA variable. If any of the rows are out of range, decide how to fix each row so that you no longer have out of range data.**

```{r}

range(college_reimport_fixed$gpa, na.rm = TRUE)


college_reimport_fixed %>%
  filter(gpa < 0 | gpa > 4)

college_reimport_fixed <- college_reimport_fixed %>%
  mutate(
    gpa = case_when(
      gpa > 4 ~ 4,
      gpa < 0 ~ 0,
      TRUE ~ gpa
    )
  )





```
```{r}

college_reimport_fixed$gpa[college_reimport_fixed$gpa == 12.96] <- NA

min(college_reimport_fixed$gpa, na.rm = TRUE)
max(college_reimport_fixed$gpa, na.rm = TRUE)

```




***

**17. Check the scale1 variable and make changes if there are any issues. (Hint: Even though this variable is an integer, there are only five values so that the *table()* function may again prove useful.)**

```{r}

# Step 1: View the frequency of values in scale1
table(college_reimport_fixed$scale1, useNA = "ifany")

# Step 2: Identify any problematic rows
college_reimport_fixed %>%
  filter(!scale1 %in% 1:5 | is.na(scale1))

# Step 3: Fix scale1 values outside the expected range (1 to 5)
college_reimport_fixed <- college_reimport_fixed %>%
  mutate(
    scale1 = if_else(scale1 %in% 1:5, scale1, NA_real_)
  )

```

```{r}


college_reimport_fixed$scale1[college_reimport_fixed$scale1 == -99] <- NA

table(college_reimport_fixed$scale1)


```


***

**18. Use similar methods to check the scale2 variable and make changes if there are any issues.**


```{r}

# Step 1: View the frequency of values in scale2
table(college_reimport_fixed$scale2, useNA = "ifany")

# Step 2: Identify any problematic rows
college_reimport_fixed %>%
  filter(!scale2 %in% 1:9 | is.na(scale2))

# Step 3: Fix scale2 values outside the expected range (1 to 5)
college_reimport_fixed <- college_reimport_fixed %>%
  mutate(
    scale2 = if_else(scale2 %in% 1:9, scale2, NA_real_)
  )




```

```{r}
table(college_reimport_fixed$scale2)
```



***

**19. Even though you already specified the class of each variable when you read in the data, check these again to make sure they are ready for analysis. (Hint: You may have cleaned up character data for variables that can now become factors.)**


```{r}

# Step 1: Check the structure of the dataset
str(college_reimport_fixed)

# Step 2: Convert appropriate character variables to factors
college_reimport_fixed <- college_reimport_fixed %>%
  mutate(
    gender = as.factor(gender),
    class = as.factor(class)
  )

# Step 3: Confirm the structure again after conversion
str(college_reimport_fixed)


```


***

**20. Write your data to a file entitled *College Study Data (Clean).csv*.**

```{r}

# Write cleaned data to CSV
write_csv(college_reimport_fixed, here("data", "College Study Data (Clean).csv"))


```




```{r}

# Read in the cleaned data.

college_clean <- read_csv(here("data", "College Study Data (Clean).csv"))

# Load the function file from the functions folder.

source(here("functions", "College Data Check.R"))

# Run the check function on your cleaned data.

College_Data_Check(college_clean)


```



***
